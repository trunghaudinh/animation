<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.trunghaudinh.learnanimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".motionlayout.SplashActivity"></activity>
        <activity android:name=".motionlayout.WriteActivity" />
        <activity android:name=".motionlayout.ReadBookActivity" />
        <activity android:name=".motionlayout.Example1Activity" />
        <activity android:name=".motionlayout.MotionActivity" />
        <activity android:name=".animation.DrawableAnimation" />
        <activity android:name=".animation.TweenAnimation" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>